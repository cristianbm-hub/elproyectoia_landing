version: '3.8'

services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./:/app:delegated
      - node_modules:/app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev -- --host

volumes:
  node_modules: 